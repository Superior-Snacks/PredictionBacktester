<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <ServerGarbageCollection>true</ServerGarbageCollection>
    <ConcurrentGarbageCollection>true</ConcurrentGarbageCollection>
  </PropertyGroup>

  <!-- Engine only: KalshiOrderClient + KalshiApiConfig (which also loads the solution-root .env).
       Deliberately NOT referencing Strategies or HardVenArb — see EVBOT_TODO.md §5. -->
  <ItemGroup>
    <ProjectReference Include="..\PredictionBacktester.Engine\PredictionBacktester.Engine.csproj" />
  </ItemGroup>

</Project>
