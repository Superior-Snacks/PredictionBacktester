# HardVen sidecar — the "private API" the C# bot calls. Book-agnostic core + pluggable adapters.
fastapi
uvicorn[standard]
httpx

# bookmaker.eu adapter is browser-driven (no API/broker needed). After installing:
#   playwright install chromium
playwright
